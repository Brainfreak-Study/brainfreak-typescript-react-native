export type UserState = {
  _id: string;
  email: string;
  username: string;
  role: string;
};

export type IUser = {
  _id: string;
  email: string;
  username: string;
  role: string;
  name: string;
  avatar: string;
};
